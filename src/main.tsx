import {createRoot} from 'react-dom/client';
import App from './App';
import './styles.css';

// у каждого эффекта теперь своя цель;
// Урон → AoE и Лечение → Замок можно использовать в одной способности;
// общая цель способности удалена;
// добавлен отдельный dropdown «Визуальный эффект»:
// Без эффекта
// Огонь
// Лёд
// Молния
// Лечение
// добавление урона к способности с замедлением больше не меняет визуал льда на молнию;
// визуальный эффект полностью независим от Урон / Замедление / Лечение;
// AoE по-прежнему срабатывает даже при отсутствии врагов;
// старый v4 JSON не поддерживается, новый формат — game.abilities.v5;
// обновлены обычные unit/storage-тесты без HTML/source-code тестов.
//
// В архиве только 10 изменённых файлов, структура папок сохранена.

createRoot(document.getElementById('root')!).render(<App/>);
