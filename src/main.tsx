import {createRoot} from 'react-dom/client';
import App from './App';
import './styles.css';

// Модалка выбора карточек теперь центрируется относительно экрана и находится выше кнопки «Главное меню». Сам игровой header не трогал — он уже position: relative, проблема была в z-index кнопки и модалки.
// «Цвет визуального эффекта» для нового periodic по умолчанию #000000. Если удалить/ввести неправильный цвет — ошибка показывается прямо под полем.
// Добавил 20 разных карточек: разные значения параметров, все 4 редкости, разные веса и все доступные типы улучшений/добавления эффектов.
// «Максимальное количество получений одной карточки» перенесено в редактор карт. Карточка больше сама этот лимит не хранит.
// Игра теперь реально применяет Добавить урон / periodic / slow / heal и переносит в способность параметры карточки без скрытых значений.
// Обычные модификаторы больше не создают отсутствующий эффект автоматически.
// Явные 0 сохраняются как 0, в том числе шанс, длительность, количество целей и размер области.
// Старый JSON специально не поддерживал.
// Тесты обновлены без проверок HTML и текста исходных файлов.
// Логику «не давать легендарные карточки на первой волне» не делал, как и договорились.

createRoot(document.getElementById('root')!).render(<App/>);
